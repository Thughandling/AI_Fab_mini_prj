# Copy to .env and fill in (never commit .env)
LLM_PROVIDER=mock
# LLM_PROVIDER=qwen        # HuggingFace Qwen3-0.6B (Colab 권장)
# LLM_PROVIDER=anthropic

HF_MODEL_ID=Qwen/Qwen3-0.6B
CLAUDE_MODEL=claude-3-5-haiku-latest
# ANTHROPIC_API_KEY=
# HF_TOKEN=                 # gated model 사용 시
